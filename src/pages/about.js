import React from 'react'
import Layout from '../components/layout'
import Head from '../components/head'

export default () => (
  <Layout>
    <Head title={'Обо мне'} />
    <h2>Обо мне</h2>
    <p>Тут пока ничего нет, но обязательно будет</p>
  </Layout>
)
